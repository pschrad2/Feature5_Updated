import React from "react";

const Account = () => {
    return (
        <div>
            <h1>You are logged in!</h1>
            <p>Welcome to your account page. Only authenticated users can see this.</p>
        </div>
    );
};

export default Account;
